{
  "name": "hrms-penguin",
  "version": "0.0.1",
  "author": "",
  "main": "index.js",

  "description": "CLI helper to make lives easier in a company",
  "license": "MIT",
  "scripts": {
    "start": "bun cli.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "bun build ./cli.js --outfile hrms-penguin --compile --minify"
  },
  "type": "module",
  "dependencies": {
    "chalk": "^5.2.0",
    "commander": "^9.4.1",
    "inquirer": "^9.1.4",
    "nanospinner": "^1.1.0",
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
